<x-filament-panels::page>
    <div class="space-y-6">
        {{-- Global Alerts Toggle --}}
        <div class="bg-white rounded-lg shadow p-6">
            <div class="flex items-center justify-between">
                <div>
                    <h3 class="text-lg font-medium text-gray-900">Global Alerts</h3>
                    <p class="mt-1 text-sm text-gray-500">
                        Master switch for all billing alerts
                    </p>
                </div>
                <button
                    wire:click="toggleGlobalAlerts"
                    class="relative inline-flex h-6 w-11 flex-shrink-0 cursor-pointer rounded-full border-2 border-transparent transition-colors duration-200 ease-in-out focus:outline-none focus:ring-2 focus:ring-primary-500 focus:ring-offset-2 {{ $globalAlertsEnabled ? 'bg-primary-600' : 'bg-gray-200' }}"
                    role="switch"
                    aria-checked="{{ $globalAlertsEnabled ? 'true' : 'false' }}">
                    <span class="pointer-events-none relative inline-block h-5 w-5 transform rounded-full bg-white shadow ring-0 transition duration-200 ease-in-out {{ $globalAlertsEnabled ? 'translate-x-5' : 'translate-x-0' }}"></span>
                </button>
            </div>
        </div>

        {{-- Alert Configurations --}}
        @if($showConfigurations)
        <div class="bg-white rounded-lg shadow">
            <div class="px-6 py-4 border-b border-gray-200">
                <h3 class="text-lg font-medium text-gray-900">Alert Configurations</h3>
            </div>
            <div class="divide-y divide-gray-200">
                @foreach($alertConfigs as $config)
                    <div class="p-6">
                        <div class="flex items-start justify-between">
                            <div class="flex-1">
                                <h4 class="text-base font-medium text-gray-900">
                                    {{ $this->getAlertTypeLabel($config['alert_type']) }}
                                </h4>
                                <p class="mt-1 text-sm text-gray-500">
                                    {{ $this->getAlertTypeDescription($config['alert_type']) }}
                                </p>
                                
                                {{-- Configuration Details --}}
                                <div class="mt-4 grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3">
                                    {{-- Enabled Toggle --}}
                                    <div class="flex items-center space-x-3">
                                        <input
                                            type="checkbox"
                                            wire:model="alertConfigs.{{ $config['alert_type'] }}.is_enabled"
                                            wire:change="updateAlertConfig('{{ $config['alert_type'] }}', { is_enabled: $event.target.checked })"
                                            class="h-4 w-4 rounded text-primary-600 focus:ring-primary-500"
                                        >
                                        <label class="text-sm text-gray-700">Enabled</label>
                                    </div>

                                    {{-- Notification Channels --}}
                                    <div class="col-span-2">
                                        <label class="text-sm font-medium text-gray-700">Channels:</label>
                                        <div class="mt-1 flex space-x-4">
                                            <label class="flex items-center">
                                                <input
                                                    type="checkbox"
                                                    value="email"
                                                    checked
                                                    disabled
                                                    class="h-4 w-4 rounded text-primary-600"
                                                >
                                                <span class="ml-2 text-sm text-gray-600">Email</span>
                                            </label>
                                            <label class="flex items-center opacity-50">
                                                <input
                                                    type="checkbox"
                                                    value="sms"
                                                    disabled
                                                    class="h-4 w-4 rounded text-gray-300"
                                                >
                                                <span class="ml-2 text-sm text-gray-500">SMS (Coming Soon)</span>
                                            </label>
                                        </div>
                                    </div>

                                    {{-- Thresholds --}}
                                    @if(in_array($config['alert_type'], ['usage_limit', 'budget_exceeded', 'overage_warning']))
                                    <div class="col-span-3">
                                        <label class="text-sm font-medium text-gray-700">Alert Thresholds (%):</label>
                                        <div class="mt-1 flex space-x-2">
                                            @foreach($config['thresholds'] ?? [80, 90, 100] as $threshold)
                                                <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-gray-100 text-gray-800">
                                                    {{ $threshold }}%
                                                </span>
                                            @endforeach
                                        </div>
                                    </div>
                                    @endif

                                    {{-- Advance Days --}}
                                    @if(in_array($config['alert_type'], ['payment_reminder', 'subscription_renewal']))
                                    <div>
                                        <label class="text-sm font-medium text-gray-700">
                                            Days in Advance: {{ $config['advance_days'] ?? 3 }}
                                        </label>
                                    </div>
                                    @endif
                                </div>
                            </div>
                            
                            {{-- Test Button --}}
                            <div class="ml-4">
                                <button
                                    wire:click="testAlert('{{ $config['alert_type'] }}')"
                                    class="inline-flex items-center px-3 py-1.5 border border-gray-300 shadow-sm text-sm font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-primary-500"
                                >
                                    Test Alert
                                </button>
                            </div>
                        </div>
                    </div>
                @endforeach
            </div>
        </div>
        @endif

        {{-- Alert History Table --}}
        @if($showHistory)
        <div class="bg-white rounded-lg shadow">
            <div class="px-6 py-4 border-b border-gray-200">
                <h3 class="text-lg font-medium text-gray-900">Alert History</h3>
            </div>
            <div class="overflow-hidden">
                {{ $this->table }}
            </div>
        </div>
        @endif
    </div>
</x-filament-panels::page>