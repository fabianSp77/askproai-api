<?php

use Filament\Http\Controllers\Auth\LoginController;
use Illuminate\Support\Facades\Route;

/*
 | Notfall-Workaround – registriert den fehlenden POST-Endpunkt.
 | Kann wieder entfernt werden, sobald ein Filament-Update die Route liefert.
 */
Route::post('admin/login', [LoginController::class, 'store'])
    ->middleware(['web', 'guest'])
    ->name('filament.admin.auth.login.post-fix');
